/*
 *
 * SimpleListStyle
 *
 */
import { StyleSheet } from 'react-native';
import { Colors } from '../../../theme';

export default StyleSheet.create({
  error: {
    textAlign: 'center',
    color: Colors.error,
  },
});
