/**
 * Test sagas
 */

// import { take, call, put, select } from 'redux-saga/effects';
// import phoenixLoginScreenSaga from '../saga';

// const generator = phoenixLoginScreenSaga();

describe('phoenixLoginScreenSaga Saga', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
