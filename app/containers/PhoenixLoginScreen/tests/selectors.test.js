// import { selectPhoenixLoginScreenDomain } from '../selectors';

describe('selectPhoenixLoginScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
